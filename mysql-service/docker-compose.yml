
mysql:
  image: mysql:5.7
  ports:
    - "3306:3306"
  volumes:
    - ./data:/var/lib/mysql
  environment:
    MYSQL_ROOT_PASSWORD: password
    MYSQL_DATABASE: grades_db
    MYSQL_USER: user
    MYSQL_PASSWORD: password
  networks:
    - app-network
